services:
  - type: free
    name: rps-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd src && gunicorn main:app -w 4 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.8.0
      - key: DATABASE_URL
        value: postgresql://postgres:jJJfPs9!JcN8tU_@db.ivzftnbuysigksgamptg.supabase.co:5432/postgres

databases:
  - name: rps-db
    databaseName: rpsdb
    user: rps_user 